services:
  mail_local:
    image: axllent/mailpit:v1.29
    container_name: bid_mailpit
    ports:
      - "8025:8025"
      - "1025:1025"
    restart: unless-stopped
    environment:
      - MP_DATA_FILE=/data/mailpit.db
    volumes:
      - ./.volumes/mailpit_data:/data

  stripe_listener:
    image: stripe/stripe-cli:latest
    container_name: bid_stripe_listener
    command: >
      listen --api-key ${STRIPE_SECRET_KEY}
      --forward-to http://host.docker.internal:5202/api/external/webhooks/stripe
    extra_hosts:
      - "host.docker.internal:host-gateway"

  ngrok:
    image: ngrok/ngrok:3-alpine
    container_name: bid_ngrok
    ports:
      - "4040:4040"
    environment:
      - NGROK_AUTHTOKEN=${NGROK_TOKEN}
    command: http host.docker.internal:5202/api/externals/webhook
    extra_hosts:
      - "host.docker.internal:host-gateway"
